<%= form_with(model: @adjustment,url: adjustment_update_admin_loan_request_path) do |form| %>
  <% if @adjustment.errors.any? %>
    <div>
      <h2><%= pluralize(@adjustment.errors.count, "error") %> prohibited this loan request from being saved:</h2>

      <ul>
        <% @adjustment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <h3>Balance : <%= current_user.wallet.balance %></h3>
  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, value: @last_adjustment.amount %>
  </div>

  <div class="field">
    <%= form.label :interest_rate %>
    <%= form.text_field :interest_rate, value: @last_adjustment.interest_rate %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div>
    <%= form.submit 'Adjust Loan Request' %>
  </div>
<% end %>
